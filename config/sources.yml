timezone: Europe/Minsk

sources:
  # Рабочий RSS Relax.by (новости — для проверки пайплайна)
  - name: "Relax.by Новости (RSS)"
    type: "rss"
    url: "https://mag.relax.by/news-rss-feed.xml"
    city: "Minsk"
    category_hint: "news"

  # Пример HTML-афиши Relax (селекторы примерные — можно уточнять)
  - name: "Relax.by Афиша (HTML)"
    type: "html"
    url: "https://afisha.relax.by/"
    list_selector: ".events__item"
    title_selector: ".events__title"
    date_selector:  ".events__date"
    place_selector: ".events__place"
    price_selector: ".events__price"
    link_selector:  "a"
    image_selector: "img"          # попробует взять из карточки; иначе — og:image со страницы
    category_hint: "concert"
